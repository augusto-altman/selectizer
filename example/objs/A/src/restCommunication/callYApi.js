define(["ajaxGet", "a_"], function(ajaxGet, a_) {
    //Independant code code code
    function callYApi() {
        //Independant code code code
        ajaxGet();
        //Independant code code code
    }
    //Independant code code code

    a_.callY = callYApi;

    return callXApi;
});